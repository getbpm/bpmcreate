bpmcreate - Create a BPM package from a url

This is a bit overkill, but it slightly automates the process which
could be useful.

    bpmcreate create URL --name="pkg_name"

To see more parameters:

    bpmcreate help create
